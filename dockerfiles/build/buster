# [build] Debian 10

ARG IMAGE="debian:buster-slim"

FROM $IMAGE AS mcode

RUN apt-get update -qq \
 && apt-get -y install --no-install-recommends \
    ca-certificates \
    gcc \
    gnat-7 \
    make \
    zlib1g-dev \
 && apt-get autoclean && apt-get clean && apt-get -y autoremove \
 && update-ca-certificates

#---

FROM mcode AS llvm-7

RUN apt-get update -qq \
 && apt-get -y install --no-install-recommends \
    clang-7 \
    llvm-7-dev \
 && apt-get autoclean && apt-get clean && apt-get -y autoremove

#---

FROM mcode AS gcc

RUN apt-get update -qq \
 && apt-get -y install --no-install-recommends \
    autogen \
    bzip2 \
    curl \
    dejagnu \
    flex \
    g++ \
    lbzip2 \
    texinfo \
    wget \
 && apt-get autoclean && apt-get clean && apt-get -y autoremove
