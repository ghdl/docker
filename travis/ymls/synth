jobs:
  include:
    - &job
      env: TASK=synth
      script: ./travis/run.sh -s "$TASK"
      deploy:
        - provider: script
          skip_cleanup: true
          script: ./travis/run.sh "$TASK"
          on:
            repo: ghdl/docker
            branch: synth
    - <<: *job
      env: TASK=pnr
    - <<: *job
      stage: formal
      env: TASK=formal
