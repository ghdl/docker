os: linux
services: docker
language: minimal
install: skip
addons:
  apt:
    packages:
      - pass

script: ./extended.sh

deploy:
  - provider: script
    skip_cleanup: true
    script: ./deploy2dockerhub.sh "$IMAGE"
    on:
      repo: ghdl/docker
      branch: ext
