os: linux
services: docker
language: minimal
install: skip
addons:
  apt:
    packages:
      - pass

before_script: git clone https://github.com/ghdl/ghdl
script: ./travis/run.sh -b

deploy:
  provider: script
  skip_cleanup: true
  script: ./travis/run.sh "$IMAGE"
  on:
    repo: ghdl/docker
    branch: buildtest

env:
  # For each linux build, a different job/instance (with the constraints
  # above) is executed in parallel in stage 'test'.
  matrix:
